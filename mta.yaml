_schema-version: "3.2"
ID: surveyform
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: surveyform-destination-content
  type: com.sap.application.content
  requires:
  - name: surveyform-destination-service
    parameters:
      content-target: true
  - name: surveyform-repo-host
    parameters:
      service-key:
        name: surveyform-repo-host-key
  - name: surveyform-uaa
    parameters:
      service-key:
        name: surveyform-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: surveyform_html_repo_host
          ServiceInstanceName: surveyform-html5-service
          ServiceKeyName: surveyform-repo-host-key
          sap.cloud.service: surveyform
        - Authentication: OAuth2UserTokenExchange
          Name: surveyform_uaa
          ServiceInstanceName: surveyform-xsuaa-service
          ServiceKeyName: surveyform-uaa-key
          sap.cloud.service: surveyform
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: surveyform-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: surveyform-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - surveyform.zip
      name: surveyform
      target-path: resources/
- name: surveyform
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: surveyform-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: surveyform-destination-service
    service-plan: lite
- name: surveyform-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: surveyform-xsuaa-service
    service-plan: application
- name: surveyform-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: surveyform-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
